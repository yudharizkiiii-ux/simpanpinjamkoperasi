import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.koperasi.simpanpinjam',
  appName: 'Simpan Pinjam',
  webDir: 'dist'
};

export default config;
